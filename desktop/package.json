{
  "name": "fugazchat-desktop",
  "version": "1.1.0",
  "private": true,
  "description": "FugazChat desktop shell (Tauri) — E2EE chat con relay Rust local y túnel Cloudflare",
  "license": "AGPL-3.0",
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "tauri build",
    "build:portable": "tauri build --no-bundle"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.11.4"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.0.0",
    "@tauri-apps/plugin-shell": "^2.0.0",
    "@tauri-apps/plugin-updater": "^2.0.0"
  }
}
